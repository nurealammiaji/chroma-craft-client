import { useState } from "react";

const Categories = () => {

    const [categories, setCategories] = useState(null);

    return (
        <div>

        </div>
    );
};

export default Categories;